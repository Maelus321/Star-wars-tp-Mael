{# templates/user/show.html.twig #}
{% extends "base.html.twig" %}



{% block body %}
<div class="profile-page">

    <div class="profile-card">
        <h1 class="profile-title">{{ user.username }}'s profile</h1>

        <div class="profile-info">
            <p><strong>ID :</strong> {{ user.id }}</p>
            <p><strong>Username :</strong> {{ user.username }}</p>
            <p><strong>Role :</strong> {{ user.roles|join(', ') }}</p>
            <p>
                <strong>Cart :</strong>
                {% if user.cart %}
                    <span class="profile-badge">
                        CART #{{ user.cart.id }} ({{ user.cart.products|length }} products)
                    </span>
                {% else %}
                    <span class="profile-badge empty">
                        No cart
                    </span>
                {% endif %}
            </p>
        </div>
    </div>

    {% if user.cart and user.cart.products|length > 0 %}
        <div class="cart-section">
            <h2 class="section-title">My cart</h2>

            <table class="cart-table">
                <thead>
                    <tr>
                        <th>Name</th>
                        <th>Price</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    {% for product in user.cart.products %}
                        <tr>
                            <td>{{ product.name }}</td>
                            <td>{{ product.price }} €</td>
                            <td>
                                <form action="{{ path('cart_remove', {'id': product.id}) }}" method="post" onsubmit="return confirm('Retirer ce produit du panier ?')">
                                    <input type="hidden" name="_token" value="{{ csrf_token('remove-from-cart-' ~ product.id) }}">
                                    <button type="submit" class="cart-remove-btn">
                                        Remove
                                    </button>
                                </form>
                            </td>
                        </tr>
                    {% endfor %}
                </tbody>
                <tfoot>
                    <tr>
                        <td><strong>Total :</strong></td>
                        <td colspan="2">
                            <strong>{{ user.cart.products|reduce((total, product) => total + product.price, 0) }} €</strong>
                        </td>
                    </tr>
                </tfoot>
            </table>

            <div class="cart-actions">
                <a href="{{ path('product_list') }}" class="profile-button">
                    Continue buying
                </a>
            </div>
        </div>

    {% elseif user.cart %}
        <div class="profile-alert">
            Your cart is empty
            <a href="{{ path('product_list') }}">Continue buying</a>
        </div>
    {% endif %}

    <div class="profile-actions">
        <a href="{{ path('user_edit', {'id': user.id}) }}" class="profile-button">
            Modify my profile
        </a>

        {% if is_granted('ROLE_ADMIN') %}
            <a href="{{ path('user_index') }}" class="profile-button secondary">
                Every users
            </a>
        {% endif %}
    </div>

</div>
{% endblock %}